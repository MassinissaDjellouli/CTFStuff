#!/bin/bash
if [ -z "$1" ]
then
    echo "Pass a directory name"
    exit
fi
mkdir "$1"
cd "$1" || exit
python3 -m venv .venv
path=$CTF_STUFF
cp "$path""examples/z3sol.py" "./z3.py"
cp "$path""examples/pwnsolve.py" "./pwnsolve.py"